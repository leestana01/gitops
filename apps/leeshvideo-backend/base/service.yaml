apiVersion: v1
kind: Service
metadata:
  name: leeshvideo-back
  labels:
    app: leeshvideo-back
spec:
  selector:
    app: leeshvideo-back
  ports:
    - name: http
      port: 8080
      targetPort: 8080
