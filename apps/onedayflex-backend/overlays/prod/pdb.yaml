# 드레인이 두 파드를 동시에 걷어가지 못하게 막는다. 최소 1개는 항상 살아 있어야
# api.onedayflex.klr.kr 이 유지된다.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: onedayflex-back
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: onedayflex-back
