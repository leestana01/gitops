apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jeju-cruise-admin
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # 운영자 전용 콘솔 — 내부망 한정. K8s 노드망 + WireGuard VPN 클라이언트만 허용
    # (argocd/jenkins/dev 인그레스와 동일 패턴).
    nginx.ingress.kubernetes.io/whitelist-source-range: "10.0.10.0/24,10.0.100.0/24"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - admin.cruise.klr.kr
      secretName: admin-cruise-klr-kr-tls
  rules:
    - host: admin.cruise.klr.kr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jeju-cruise-admin
                port:
                  number: 80
