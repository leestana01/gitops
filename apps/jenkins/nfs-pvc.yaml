apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-nfs
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
  volumeName: jenkins-nfs
