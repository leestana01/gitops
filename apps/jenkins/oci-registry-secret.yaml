apiVersion: v1
kind: Secret
metadata:
  name: oci-registry-secret
  namespace: jenkins
type: kubernetes.io/dockerconfigjson
data: {}
# To create this secret, run:
# kubectl create secret docker-registry oci-registry-secret -n jenkins \
#   --docker-server=yny.ocir.io \
#   --docker-username='axlgn2n9ijoa/oracleidentitycloudservice/YOUR_EMAIL' \
#   --docker-password='YOUR_AUTH_TOKEN'
#
# Replace YOUR_EMAIL and YOUR_AUTH_TOKEN with actual credentials.
# This placeholder file documents the required secret for reference.
