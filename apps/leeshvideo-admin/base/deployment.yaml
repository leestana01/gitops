apiVersion: apps/v1
kind: Deployment
metadata:
  name: leeshvideo-admin
  labels:
    app: leeshvideo-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: leeshvideo-admin
  template:
    metadata:
      labels:
        app: leeshvideo-admin
    spec:
      containers:
        - name: leeshvideo-admin
          image: yny.ocir.io/axlgn2n9ijoa/leeshvideo/admin
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              cpu: "25m"
              memory: "32Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
      imagePullSecrets:
        - name: ocir-secret
