apiVersion: v1
kind: PersistentVolume
metadata:
  name: mail-nfs
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - nolock
    - nfsvers=3
  nfs:
    server: 10.96.78.207
    path: /shared
