apiVersion: v1
kind: PersistentVolume
metadata:
  name: mail-nfs
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.0.30.45
    path: /shared
