apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mail-server
  namespace: mail
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - admin.webmail.klr.kr
      secretName: admin-webmail-klr-kr-tls
  rules:
    - host: admin.webmail.klr.kr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mail-server
                port:
                  number: 8080
